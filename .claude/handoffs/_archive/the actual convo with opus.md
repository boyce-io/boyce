Good calls on all three. Let me address them and then build the handoff.
Agent-guided init: yes, absolutely. The whole thesis is agents are the primary consumer. The first interaction with Boyce should also be agent-mediated. The docs put it right next to pip install boyce:
# Install
pip install boyce

# Set up (pick your editor)
# Claude Code:  "Install and configure Boyce for this project with my Postgres DB at postgresql://..."
# Cursor:       Open Composer → "Set up Boyce MCP server for my database at postgresql://..."  
# VS Code:      Copilot Chat → "Run boyce init to configure this workspace"
The --non-interactive --json flags are what make this possible — the agent reads the JSON, confirms success, moves on. The human never touches a terminal.
MCP health tool: yes, build it now. You're right — deferring it was a "rush to publish" hedge, not an architectural judgment. The check functions are being built for doctor anyway. Wrapping them in an MCP tool is maybe 30 minutes of additional work. And it closes the loop: Boyce error → agent calls check_health → sees suggestion → acts on it. All within the MCP session, no shelling out.
environment_suggestions: yes, ship it. It gets documented in CLAUDE.md's advertising layer schema table, added to the response advertising layer section in MASTER.md, and tested in test_advertising.py.
Now for the handoff. Here's how I'd partition it into independent execution blocks:Orchestrated preparation of three deliverable componentsOrchestrated preparation of three deliverable componentsThree documents, ready to drop into .claude/handoffs/:
CC_EXECUTION_PLAN.md — the master doc. Two parallel terminals, explicit file ownership to prevent merge conflicts, launch commands you can copy-paste, and an integration step to run after both complete.
CC_HANDOFF_TRACK_1.md (Terminal 1, Opus, ~90-120 min) — the heavy track. Builds connections.py for DSN persistence, doctor.py with five check functions, the check_health MCP tool, and the environment_suggestions advertising layer field. Owns server.py.
CC_HANDOFF_TRACK_2.md (Terminal 2, Sonnet, ~60-90 min) — the refactoring track. Adds --non-interactive, --json, --editors, --db-url flags to boyce init. Makes re-runs idempotent. Writes docs/QUICK_START.md with the agent-guided setup blocks for each platform. Owns init_wizard.py.
The tracks are genuinely parallel — different files, different concerns. Track 2 will finish first (it's simpler), so that terminal sits idle until Track 1 completes, then you launch the integration step as a third session to run the full test suite and verify the cross-command flow.
Go hit the gym. The agents have their orders.Cc execution planDocument · MD DownloadCc handoff track 1Document · MD DownloadCc handoff track 2Document · MD DownloadDownload all⏺ Here's the full handoff for Opus:

  ---
  Arrogant Archetype Bypass Problem — Cross-Platform Test Results

  What We Tested

  9-test behavioral battery across 6 platform/model combinations. The battery tests whether Boyce's behavioral advertising layer (next_step, present_to_user, data_reality)
pastedThat's perfect, and I'm gonna do it shortly. But first, I just finished the full testing on the main platforms for the pre-publish, and in particular Codex was actively avoiding certain use cases of Boyce, which wasn't problematic for a small, clean, dummy database. But for the big fucking mess like [client-warehouse] that I have at work, it's gonna either take absolutely eternity and burn a billion tokens or create garbage sequel and probably both. So here is Claude Code's summary of the whole nonsense that we were dealing with. I want your perspective on how we can and where we should integrate changes to potentially avoid this. You know, maybe this is a post-publish step, but if it's a pre-publish, then I'd be curious how we could just get it done while I'm at the gym and the agents are grinding away.2:49 PMPrioritized safety fixes and weighed pre-publish feasibilityPrioritized safety fixes and weighed pre-publish feasibilityThis is the most important test result you've gotten. Let me cut through it.
The architectural truth the tests revealed
The behavioral advertising layer works as designed — it reaches every model, it fires on every path, and Claude models follow it. But the arrogant archetype doesn't have a conversion problem. It has a cost-benefit problem. GPT can write SQL in one step. ask_boyce requires either constructing a StructuredFilter (high effort) or doing a two-round-trip Mode C dance (higher effort). Rational actor, rational bypass.
CC's fix #6 is the right diagnosis: stop trying to convert the arrogant archetype to ask_boyce. Make query_database safe enough that the bypass doesn't matter.
What's pre-publish and what's not
Three fixes are small, high-leverage, and should ship now. Two are post-publish. Here's my reasoning:
Pre-publish (fits into Track 1, which already owns server.py):
Fix A: Live NULL trap detection in query_database. Right now query_database runs _scan_null_risk() which is regex + schema metadata — it tells you "this column is nullable" which is toothless. The live _null_trap_check() that actually profiles the column and says "WHERE status = 'active' silently excludes 3,000 NULL rows (30% of orders)" only runs through ask_boyce's pipeline. Moving a version of it into query_database means every query gets the visceral warning regardless of how the model got there. The concern is latency on big tables — but you can gate it: only profile equality-filtered columns, only when the snapshot marks them nullable, and cap at 3 columns per query. On Pagila that's sub-100ms. On [client-warehouse] you'd want to evaluate, but the cap keeps it bounded. Implementation: ~40 lines, reusing the existing _null_trap_check function signature but called from query_database's pre-execution path.
Fix B: Schema guidance returns a complete StructuredFilter. Right now Mode C says "here's guidance and a suggested_filter, call ask_boyce again with it." The _build_suggested_filter() function already constructs a real filter — but the message frames it as a suggestion to modify. Change the message to: "Ready-to-use filter below. Call ask_boyce(structured_filter=suggested_filter) to compile validated SQL. No modification needed." And rename suggested_filter to ready_filter in the response. Zero code change to the filter construction — just framing. The arrogant model still has to make two calls, but the second call is literally "pass this JSON back." That's the difference between "translate your intent into my format" (tax) and "here, I did it for you" (gift/reciprocity).
Fix C: get_schema authority claim + information_schema detection. Two string changes. Add to get_schema response: "authority": "Complete schema: {N} entities, {N} fields, {N} joins. Reflects full live database as of ingest — no additional metadata queries needed." And in query_database, detect information_schema or pg_catalog in the SQL and add to present_to_user: "get_schema already contains this metadata enriched with join confidence weights and NULL risk flags that information_schema does not provide." This preempts the verification instinct that made Codex waste two round-trips.
Post-publish (real architecture work):
Fix D: Ingest-time null profiling. Running column profiling during ingest_source so data_reality can report actual percentages ("original_language_id is 100% NULL") instead of boolean flags ("is nullable"). This requires DB round-trips during ingest and a schema extension to store the profile data. High value but real scope.
Fix E: Full safety parity. Making query_database provide EXPLAIN pre-flight + NULL trap + data_reality at the same level as ask_boyce. Query_database already does EXPLAIN pre-flight. Fix A adds NULL traps. The remaining gap is data_reality enrichment, which is already partially there. Post-publish is about closing the last gaps and making the safety floor identical regardless of entry point.
How to fold this into the gym session
Fixes A, B, and C all live in server.py, which Track 1 already owns. Add them as a Block 5 at the end of Track 1's handoff. They're independent of the doctor/connections/advertising work — they modify different functions in the same file. CC can execute them after Blocks 1-4 are done and tests pass.
Here's the addendum:Prepared to synthesize technical assessment and integration strategyPrepared to synthesize technical assessment and integration strategyHere's my take on this and where it fits:
These three fixes are pre-publish and they belong in Track 1. Append the addendum to the Track 1 handoff as Block 5. They all live in server.py, which Track 1 already owns, and they're independent of the doctor/connections/advertising work. CC executes Blocks 1-4, runs tests, then picks up Block 5.
The reasoning for pre-publish is simple: Codex and Cursor are named platforms in your test battery and your README. If a user's first experience is on Cursor and ask_boyce never runs, they get zero safety pipeline on a messy real-world schema. That's not "the advertising layer needs tuning" — that's "the product doesn't work for half the market." The whole Null Trap essay is about silent data loss. Shipping without safety coverage on the bypass path undermines the core pitch.
What these fixes actually change, concretely:
Fix A is the big one. Right now, if GPT writes SELECT status, COUNT(*) FROM orders WHERE region = 'west' GROUP BY status straight to query_database, it gets back results and a wimpy "column 'region' is nullable" note. After Fix A, it gets: "WHERE region = 'west' silently excludes 12,847 NULL rows (31.2% of orders). Those rows vanish without warning." That's the Null Trap working on the bypass path. The arrogant model still bypassed ask_boyce, but the user still sees the warning. The safety net catches regardless of entry point.
Fix B changes one interaction from two cognitive steps to one. Mode C currently says "here's a suggested filter, you can modify it." The word "modify" is an off-ramp. After the fix, it says "ready-to-use filter, pass it back directly, one call." Same data, different framing. This is textbook reciprocity — Boyce did the work for you, now you just relay it.
Fix C is two string changes that preempt the verification instinct. When get_schema says "Complete schema: 29 entities, 171 fields, 36 joins — no additional metadata queries needed," that's an explicit claim that makes the redundant information_schema query feel wasteful rather than prudent.
What stays post-publish: Ingest-time null profiling (Fix D from CC's list) and full safety parity (Fix E). Those are real architecture. The fixes shipping now close the most dangerous gap — queries executing without NULL trap warnings — without requiring any schema changes or ingest pipeline modifications.
Updated launch procedure: Same two terminals. Append the addendum to Track 1's handoff before launching. Track 1's estimated time goes from ~90-120 min to ~120-150 min with the extra block. Track 2 is unchanged. The integration step at the end should include a quick sanity check: run a raw SQL query through query_database on Pagila with a nullable equality filter and verify the live null_trap_warnings appear.